# TASK-0016: Decision-record spec, ACM v1 runtime extensions, evidence pack
Gate: G-P3a
Design rules touched: 1, 2, 3
Goal: Decision-record specification and draft schema validated against real mesa-pep output; observed-edge event schema; evidence-pack specification (built from CI artifacts only); ACM v1 runtime-extension proposal; paper-three structure.
Out of scope: calibration reports (need a pinned model and owner-labelled data); standards submission (owner); paper framing (owner).
Acceptance checks (must be merged before implementation):
  - tests/test_runtime_schemas.py: mesa-pep records and observed-edge events validate; missing mandatory field rejected; final never weaker than deterministic
Protected paths: schemas/**, fixtures/**, tests/**, tasks/**, gates/**
Evidence required: evidence/TASK-0016.json
