# TASK-0002: Derivation mapping specification (Week 2 spike)
Gate: G2
Design rules touched: 4 (derived, not asserted), 5 (over-inclusion beats omission)
Goal: Write the mapping from Tier 0 artifacts (Terraform state, IAM authorization export, MCP configs, egress allowlists, secrets grants, classification tags) to topology v0.1, list the facts that can never be derived, and pre-register the T4 derivation-accuracy targets before any derivation run.
Out of scope: the derive implementation (TASK-0003, G3), graded levels in the scanner (G6/G7), and the timed hand-derivation itself (owner measurement; it feeds a Section 14 kill criterion).
Acceptance checks (must be merged before implementation):
  - docs/DERIVATION.md defines every derivation source in roadmap Section 5 with a rule, a confidence class, and a provenance pointer format
  - docs/NEVER_DERIVABLE.md lists each fact that needs a human, with the reason and who supplies it
  - gates/g2.yaml pre-registers the T4 metrics and targets; its commit timestamp precedes the first derivation run
  - docs/G2_SPIKE.md gives the owner a timed hand-derivation worksheet with blank time fields
  - make verify stays green; no scanner code changes
Protected paths: tasks/**, gates/**
Evidence required: evidence/TASK-0002.json
