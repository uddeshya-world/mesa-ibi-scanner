# TASK-0013: Upward-only advisory adapter (Jev J1-J3)
Gate: G-P2b
Design rules touched: 1 (deterministic authority), 2 (upward-only models)
Goal: One adapter through which every advisory model call passes, enforcing P-09 in code (raise-only levels, no ALLOW over HOLD/BLOCK, malformed answers ignored), P-15 (fail closed in staging, production and regulated; research fails open with a logged record), payload minimisation and a per-call record (pinned version, question wording hash, probability, threshold, margin, latency).
Out of scope: any real model client, thresholds from calibration data, the jev-eval harness (mesa-lab), the Rust PEP port (mesa-pep).
Acceptance checks (must be merged before implementation):
  - tests/test_advisory.py: P-09 on 10,000 random and adversarial model outputs (seed 7), level proposals raise-only, P-15 per zone, timeout by latency, payload minimisation, record fields, deterministic BLOCK skips the model
Protected paths: tests/**, tasks/**
Evidence required: evidence/TASK-0013.json
