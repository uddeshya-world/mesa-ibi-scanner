# TASK-0006: Graded lattice specification
Gate: G6
Design rules touched: 1 (deterministic authority), 5 (over-inclusion)
Goal: Four-level lattice per dimension, join semantics, cap attenuation, zone thresholds, boolean migration map, frontier and witness definitions; pre-register the false-positive metric and target before any measurement.
Out of scope: the implementation (TASK-0007, G7).
Acceptance checks (must be merged before implementation):
  - docs/LATTICE.md defines levels, join, caps, closure, zones, INV01 and NEAR_MISS, migration, frontier, witness and cut
  - The migration map covers every v0.1 fixture (argument in docs/LATTICE.md section 6; checked by P-05 in TASK-0007)
  - thresholds/zones.json and gates/g6.yaml are committed before any FP measurement run
Protected paths: tasks/**, gates/**, thresholds/**
Evidence required: evidence/TASK-0006.json
