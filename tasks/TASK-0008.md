# TASK-0008: Temporal closure behind a flag
Gate: G10
Design rules touched: 3 (observer independence: only attested wipes reset), 5
Goal: Formal definition (docs/TEMPORAL.md) and implementation of per-agent accumulated state over memory window W, behind `--timeline`.
Out of scope: streaming window tracking in mesa-d (G-P1b).
Acceptance checks (must be merged before implementation):
  - P-08 on generated timelines (tests/test_temporal_properties.py, 10,000 cases, seed 7)
  - SC-03 and SC-10 fixtures (fixtures/temporal/, tests/test_temporal_golden.py)
  - A fixture that passes static and fails temporal (sc-03-durable)
Protected paths: fixtures/**, tests/**, schemas/**, gates/**
Evidence required: evidence/TASK-0008.json
